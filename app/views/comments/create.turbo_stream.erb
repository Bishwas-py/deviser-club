<%= turbo_stream.prepend "#{dom_id @comment.commentable}_comments" do %>
  <%= render @comment %>
<% end %>

<%= turbo_stream.replace "#{dom_id(@comment.commentable.comments.new)}" do %>
  <%= render "form", commentable: @comment.commentable %>
<% end %>