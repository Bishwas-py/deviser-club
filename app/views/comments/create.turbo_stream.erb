<%= turbo_stream.prepend "#{dom_id @comment.commentable}_comments" do %>
  <%= render @comment %>
<% end %>

<%= turbo_stream.replace @comment.commentable.comments.new do %>
  <%= render "comments/form", commentable: @comment.commentable %>
<% end %>