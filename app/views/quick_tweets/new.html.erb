<%= render "components/errors", error_el: @quick_tweet %>

<div class="grid place-items-center bg-blue-100 min-h-screen md:px-48">
  <%= form_with(model: @post, class: "w-full rounded-xl overflow-hidden") do |form| %>
    <%= render "components/editor/edit", field: :quick_tweet, form: form, placeholder: "Write a new quick tweet" %>
  <% end %>
</div>

<style>
  .ProseMirror {
      min-height: 50vh;
      max-height: 70vh;
      overflow-y: scroll;
  }
</style>